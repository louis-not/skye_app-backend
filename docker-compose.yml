services:
  skye-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: skye-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
